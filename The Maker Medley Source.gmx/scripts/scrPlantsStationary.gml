///scrPlantsStationary(plantObject);
//Determines if a plant should be given a row/column value and laced in the "grid"
switch(argument[0]){
     case objPlantsShuriken:
        return (false);
     break;
}
return (true);
