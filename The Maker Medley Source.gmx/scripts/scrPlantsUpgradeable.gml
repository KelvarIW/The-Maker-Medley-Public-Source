///scrPlantsUpgradeable(targetObject, upgradeObject);
var _target = argument[0];
var _upgrade = argument[1];

if (instance_exists(_target)){
    var _targetObj = (_target.object_index);
    switch(_upgrade){
        /*case objPlantsSpike:
           if (_targetObj == objPlantsCannon){
               return (true);
           }
        break;*/
        default:
            return (false);
        break;
    }
    return (false);
}
return (false);
